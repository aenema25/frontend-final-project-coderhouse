export type ProductType = {
    _id: string,
    id: string
    title: string,
    description: string,
    price: number,
    code: string,
    stock: number,
    status: boolean,
    category: string,
    thumbnails: string[]
}